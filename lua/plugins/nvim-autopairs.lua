return {
  "windwp/nvim-autopairs",
  enabled = true,
  event = "InsertEnter",
  config = true,
  -- opts = opts,
  -- use opts = {} for passing setup options
  -- this is equalent to setup({}) function
}
